# Application name
spring.application.name=jobportal

# Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/jobportal?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=oppof19pro
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Thymeleaf settings (optional, improves dev speed)
spring.thymeleaf.cache=false

# Error details (for debugging only — disable in production)
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always
